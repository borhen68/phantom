# Procedure Patterns

Good reusable procedures have:

- stable step order
- explicit targets or selectors
- expected outcomes after each important action
- known approval points
- a clear recovery point when drift happens

Weak procedures usually fail because they are:

- vague about where an action happens
- missing verification
- overloaded with one-off context
- unsafe to replay without human review
