# Messaging Pairing Policy

Default PHANTOM messaging stance:

- unknown senders do not trigger work immediately
- they receive a short pairing code
- an operator approves the pairing before the sender is allowed

Open mode exists for trusted environments, but it should be deliberate and visible in `doctor`.
